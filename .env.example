# Shared trip password. Compared case-insensitively + trimmed by
# /api/auth/login. For the Spain 2026 trip, the value is a city name.
TRIP_PASSWORD=

# Supabase (used by existing booking screens)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
